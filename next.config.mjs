/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['images.abubakarcodingwala.netlify.app'],
        domains:['images.working666.vercel.app']
    }
};

export default nextConfig;
